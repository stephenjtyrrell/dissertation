apiVersion: v1
kind: Service
metadata:
  name: smoke-api
  labels:
    app.kubernetes.io/name: smoke-api
    app.kubernetes.io/part-of: dissertation-platform
    owner: platform-team
    compliance: baseline
spec:
  selector:
    app: smoke-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
